apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: samplejava-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: samplejava.ccoe.20.249.205.175.nip.io
    http:
      paths:
      - path: /
        backend:
          service:
            name: samplejava-svc
            port:
              number: 80
        pathType: Prefix
